
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function SDKDemoPanel() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>SDK Demo</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">SDK demo functionality coming soon</p>
      </CardContent>
    </Card>
  );
}
